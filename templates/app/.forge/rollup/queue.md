# Rollup Queue

Framework-wide lessons queued by the `retro` agent for upstream propagation via `/forge-rollup`.

This file is **committed** to the app repo (so it survives across builds and machines).
After `/forge-rollup` opens a PR, the queue is moved to `.forge/rollup/applied-<date>.md` and reset.

---

(empty)
